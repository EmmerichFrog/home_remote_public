App(
    appid="home_remote",  # Must be unique
    name="Home Remote",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="home_remote_app",
    stack_size=4 * 1024,
    requires=["bt", "gui"],
    order=10,
    fap_icon="app.png",
    fap_category="Misc",
    fap_icon_assets="assets",
)
